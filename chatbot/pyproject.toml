[project]
name = "handle-chatbot"
version = "0.1.0"
description = "Maintainer's Copilot Streamlit UI"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.40.0",
    "httpx>=0.27.0",
    "sseclient-py>=1.8.0",
]

[tool.uv]
package = false
